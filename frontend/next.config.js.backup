/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',
  distDir: '.next',
  assetPrefix: 'https://amirez.info',
  experimental: {
    outputFileTracingRoot: process.env.NODE_ENV === 'production' ? 
      '/root/Arbitrage_NextJs/frontend' : undefined,
  }
}

module.exports = nextConfig
